# permissions for end users to view chaosissuers.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: clusterrole
    app.kubernetes.io/instance: chaosissuer-viewer-role
    app.kubernetes.io/component: rbac
    app.kubernetes.io/created-by: venafi-test-wizard
    app.kubernetes.io/part-of: venafi-test-wizard
    app.kubernetes.io/managed-by: kustomize
  name: chaosissuer-viewer-role
rules:
- apiGroups:
  - self-signed-issuer.chaos.ch
  resources:
  - chaosissuers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - self-signed-issuer.chaos.ch
  resources:
  - chaosissuers/status
  verbs:
  - get
